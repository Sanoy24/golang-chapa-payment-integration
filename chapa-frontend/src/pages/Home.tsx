import PaymentForm from "../componenet/PaymentForm";

const Home = () => {
    return (
        <div className="flex items-center justify-center min-h-screen">
            <PaymentForm />
        </div>
    );
};

export default Home;
